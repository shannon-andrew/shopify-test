@font-face {
  font-family: "Leitura";
  src: url("./leitura-reg.woff2") format("woff2"),
    url("./leitura-reg.woff2") format("woff2");
}

@font-face {
  font-family: "Nitti";
  src: url("./nitti-light.woff2") format("woff2"),
    url("./nitti-light.woff2") format("woff2");
}

html {
  box-sizing: border-box;
  font-size: 10px;
}

*,
*:before,
*:after {
  box-sizing: inherit;
}

body,
h1,
h2,
h3,
h4,
h5,
h6,
p,
ol,
ul {
  margin: 0;
  padding: 0;
  font-weight: normal;
  font-family: "Leitura";
}

ol,
ul {
  list-style: none;
}

img {
  max-width: 100%;
  height: auto;
}

h1 {
  font-size: 8rem;
}

h2 {
  font-size: 6.4rem;
}

h3 {
  font-size: 4rem;
}

h4 {
  font-size: 3.2rem;
}

p {
  font-size: 1.6rem;
  line-height: 2.4rem;
  font-family: "Nitti";
}

p.medium {
  font-size: 2.4rem;
  line-height: 3.2rem;
}

p.large {
  font-size: 3.2rem;
  line-height: 4rem;
}

a {
  text-decoration: none;
  color: inherit;
}

body {
  margin-top: 80px;
}

button {
  background: #141414;
  color: #fff;
  padding: 12px 18px;
  border: none;
  border: 1px solid #141414;
  border-radius: 28px;
  cursor: pointer;
  transition: 0.3s;

  &:hover {
    background-color: #fff;
    color: #141414;
  }
}

.max-width {
  max-width: 1440px;
  margin: 0 auto;
  padding: 0 2.4rem;
}

.collection-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 1.6rem;
}
