{{ 'section-collection-grid.css' | asset_url | stylesheet_tag }}

{% if section.settings.collection == blank %}
  {% assign collection = collections['just-landed'] %}
{% else %}
  {% assign collection = collections[section.settings.collection] %}
{%- endif %}

<div class="max-width ">
  <div class="container">
    {% if section.settings.grid_heading or section.settings.grid_sub %}
      <div class="titles">
        <h2>
          {{ section.settings.grid_heading }}
        </h2>
        <p class="medium">
          {{ section.settings.grid_sub }}
        </p>
      </div>
    {%- endif %}
    <div class="collection-grid">
      {% for product in collection.products limit: section.settings.number %}
        {% render 'product-card', product: product %}
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Collection grid",
  "tag": "section",
  "default": {
    "settings": {
      "grid_heading": "Slideshow",
      "grid_sub": "Grid sub"
    }
  },
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Choose collection"
    },
    {
      "type": "range",
      "id": "number",
      "min": 4,
      "max": 16,
      "step": 4,
      "label": "Amount of products",
      "default": 4
    },
    {
      "type": "text",
      "id": "grid_heading",
      "label": "Grid heading"
    },
    {
      "type": "text",
      "id": "grid_sub",
      "label": "Grid sub"
    }
  ]
}
{% endschema %}
