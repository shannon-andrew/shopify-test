{{ section.settings.collection.title }}

{% for product in collections['section.settings.collection'] %}
  {% render 'product-card', product: product %}
{% endfor %}

{% schema %}
{
  "name": "Collection grid",
  "tag": "section",
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Choose collection"
    }
  ],
  "presets": [
    {
      "name": "Collection grid",
      "settings": {
        "collection": "just-landed"
      }
    }
  ]
}
{% endschema %}
