<div class="container">
  <div>
    {% for link in linklists.footer.links %}
      <a href="{{ link.url }}">
        {{ link.title }}
      </a>
    {% endfor %}
  </div>

  <div style="background:blue;">
    &copy; Shopify Theme Lab THEMEMEM
  </div>
</div>

{% schema %}
{
  "name": "footer"
}
{% endschema %}